@props(['title' => '', 'subtitle' => ''])

<div class="mb-4 mb-lg-5">
    <div class="text-secondary small text-uppercase fw-semibold">{{ $subtitle }}</div>
    <h1 class="h3 mb-0 text-dark">{{ $title }}</h1>
</div>
